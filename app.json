{
  "buildpacks":  [
    {"url": "heroku/python"},
    {"url": "heroku/procfile"}
  ],
  "stack": "heroku/buildpacks:20",
  "environments": {
    "test": {
      "scripts": {
        "test": "python -m unittest tests.py"
      }
    }
  }
}
