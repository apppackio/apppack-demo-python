{
  "buildpacks":  [
    {"url": "heroku/python"},
    {"url": "heroku/procfile"}
  ],
  "stack": "heroku/builder:22",
  "environments": {
    "test": {
      "scripts": {
        "test": "python -m unittest tests.py"
      }
    }
  }
}
